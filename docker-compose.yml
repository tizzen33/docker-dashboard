version: "3"

services:
  docker-dashboard:
    container_name: docker-dashboard
    build:
      context: .
    ports:
      - "8091:8091"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
